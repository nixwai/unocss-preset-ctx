{
  "name": "core",
  "version": "0.0.1",
  "scripts": {
    "preset:release": "gulp -f build/gulp-release.ts",
    "preset:build": "gulp -f build/gulp-build.ts",
    "preset:publish": "gulp -f build/gulp-publish.ts"
  },
  "devDependencies": {
    "@types/gulp": "catalog:",
    "gulp": "catalog:",
    "gulp-cli": "catalog:",
    "rollup-plugin-copy": "catalog:",
    "sucrase": "catalog:",
    "vite": "catalog:",
    "vite-plugin-dts": "catalog:"
  }
}
